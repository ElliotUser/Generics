                                Обобщенный подкласс.
    Суперклассом для обобщенного класса вполне может служить и необобщенный класс.
Пример.
    Ниже приведен результат выполнения данной программы.

Welcome 47

    Обратите внимание, каким образом в этой программе класс Gen наследуется от класса NonGen:
    class Gen<T> extends NonGen {
    Класс NonGen является необобщенным, поэтому никаких аргументов типа в нем не указывается. И даже если в
классе Gen объявляется параметр типа Т, то он не требуется (и не может бьть использюван) в классе NonGen.
Таким образом, класс Gen наследуется от класса NonGen обычным образом. Никаких специальных условий для этого
не требуется.